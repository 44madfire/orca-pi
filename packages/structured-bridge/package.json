{
  "name": "@orca-pi/structured-bridge",
  "version": "0.1.0",
  "description": "SNC1.3 hot-swappable external structured-session bridge + SNC1.4 Pi-backed provider (pi --mode rpc via @orca-pi/pi-rpc). Dev/test seam only; Orca owns journal/lease/outbox/rendering.",
  "type": "commonjs",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "docs"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test"
  },
  "dependencies": {
    "@orca-pi/pi-rpc": "0.1.0"
  }
}
